{
  "name": "whatagentsbuy",
  "private": true,
  "type": "module",
  "description": "Serverless functions for whatagentsbuy.com. The site itself is static HTML generated by build.py; these handle the paid x402 endpoints, which a static file cannot.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test api/*.test.js"
  }
}
